<div class="container">
	<div class="row">
		<div class="col-md-6">
			<h1>Editing Amendment</h1>
			<%= render 'form' %>
			<%= link_to 'Back', "/resolutions/#{params[:resolution_id]}" %>
		</div>
		<div class="col-md-5 col-md-offset-1">
			<h1>Resolution</h1>
			<h4>Title: <%= @resolution.title %></h4>
			<h4>Body: <%= raw @resolution.body %></h4>
		</div>
	</div>
</div>

