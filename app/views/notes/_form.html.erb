<%= form_for(@note) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Subject" %> <br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </div>
  <div class="field">
    <%= f.hidden_field :sender, value: @user.id %>
  </div>
  <div class="field">
    <%= f.label "To" %><br>
    <%= f.collection_select :reciever, User.all, :id, :country %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
